{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -c \"import uvicorn; from fastapi import FastAPI; app = FastAPI(); app.add_api_route('/', lambda: 'XSEMA Running'); app.add_api_route('/health', lambda: {'status': 'healthy'}); uvicorn.run(app, host='0.0.0.0', port=int('$PORT' or '8000'))\"",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 10,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 2
  }
}
